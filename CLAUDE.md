# Claude Code Project Note

This is a **project-scoped supplement** to any user/global Claude instructions; do not modify or replace global instruction/configuration files.

Read and follow `AGENTS.md` for Palembang-specific rules. For ready-to-use graphics, prefer `assets/canonical/png/` and select through `assets/canonical/manifest.json`. For implementation truth, use `source/Palembang.fig`, `docs/specification.md`, and `tokens/palembang.v1.json`; do not infer canonical geometry from the artwork photographs.
